# Analytics — replace placeholders with real IDs.
# Scripts only load when the value is not a placeholder, so Lighthouse
# stays clean until real IDs are in place.
NEXT_PUBLIC_GA4_ID=G-XXXXXXXXXX
NEXT_PUBLIC_META_PIXEL_ID=000000000000000
